node_modules
dist
.env
*.crt
*.pem
*.key
*.local.md
docs/*
!docs/adr/
.worktrees
.desloppify
.superpowers
.codex

# Champion data pipeline
data/raw/*
!data/raw/.gitkeep
!data/raw/cdragon-champions.json
data/compiled/*
!data/compiled/.gitkeep
# Engine reads these at runtime; must be committed to ship in Railway's git context.
!data/compiled/champion-meta.json
!data/compiled/matchup-data.json
# Navigator FM evaluator (design docs/designs/fm-evaluator-ship-design.md §1). Without
# these two lines prod silently stays on the legacy compStrength behind one warning line.
!data/compiled/fm-weights.json
!data/compiled/fm-parity.json

# Rust
target/
*.node
packages/engine-node/index.d.ts

# Generated protocol artifacts
packages/engine-protocol/schema.json
packages/engine-protocol/dist/
packages/engine-core/src/protocol_types.rs
docs/spikes/v*-data/

# Database orphan-cleanup exports (row backups, never committed)
tmp/

# custom-model training data + python env (scripts/model)
data/training/
scripts/model/.venv/
__pycache__/
