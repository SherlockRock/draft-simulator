Canvas Functionality:
    replace draft list button Functionality with right click menus?
    editing from the manage canvas' dialog
    Export draft
    folder heirarchy
    enforce unique names when copying drafts
    no permissions to move drafts but was allowed to copy
    seperating canvas drafts from "real" drafts
        "view" button needs to change in tandem with this
    manage connection type/color?
        allow for text or champion icons to identify meaning of branch
    move controls to top center?
    when traversing canvas auto minimize left menu?
    clicking to select a champ from the dropdown keeps the dropdown open
    canvas selector should use searchable select
    Canvas creation coming soon! in the style of draft creation

Short Term:
    convert to standalone functionality is a bit wonky
    draft creation public briefly went to true before navigating away
    anon user functionality is broken
    clean up panel ui
    customizable icons for activities
    delete draft functionality
    weird auth flow when a user was signed in but became outdated
    searchable select need to fix arrow key controls
    server tweaks when json token is too old? then draft wasn't loading for some reason
    Bottom of champ list doesn't display right when uneven number of champs
    Allow non signed in users to see a non saving draft
    need to handle all cases where res.ok is false
    fix or remove current user count
    new draft button disabled when viewing new draft screen

Long Term:
    fearless/iron man
    convert APIs to use Tanstack Query + better error handling
    VS draft
    View multiple drafts at once. Canvas?
    Friend Functionality -> Add ability to modify list of users who can view the draft
    Add types w/ zod
    Light mode/dark mode/UI customization?
    UI cleanup + animations
    optimize db writes for socket updates, redis?

UI colors:
bg dark slate-800
bg medium slate-700
bg light slate-600

border lighter than light slate-500

text primary slate-50
text muted slate-200

primary action dark teal-700
primary action light teal-400

danger red-500
warning yellow-500
success green-500
info